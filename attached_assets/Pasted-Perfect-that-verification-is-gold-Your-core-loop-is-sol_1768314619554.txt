Perfect — that verification is gold. Your core loop is solid, and you’re exactly at the right “next” milestone:
Next = NeoBlocks become artifacts with internal structure
(two missing pieces you identified: expand/collapse + MOLT snapshot at compression)
Below is a strict-TS-safe implementation in small code chunks that won’t blow up your build.
NEXT CHUNK 1 — Add MOLT snapshot to NeoBlock (strict TS safe)
1) Update src/umg/types.ts
Add these types (near your existing types):
Copy code
Ts
export type MoltRole = "TRIGGER" | "DIRECTIVE" | "INSTRUCTION" | "SUBJECT";

export type MoltSnapshot = Record<MoltRole, { title: string; content: string }>;
Then update NeoBlock to include snapshot:
Copy code
Ts
export type NeoBlock = {
  id: string;
  sourceBlockIds: string[];
  createdAt: number;
  label: string;
  snapshot: MoltSnapshot; // <-- add this
};
✅ This is strict-safe and avoids any.
2) Update compressToNeoBlock() in src/umg/store.ts
Inside compressToNeoBlock, right before constructing the NeoBlock, add:
Copy code
Ts
const getBlock = (role: "TRIGGER" | "DIRECTIVE" | "INSTRUCTION" | "SUBJECT") => {
  const b = blocks.find(x => x.role === role);
  if (!b) throw new Error(`Missing role during compress: ${role}`);
  return b;
};

const snapshot = {
  TRIGGER: { title: getBlock("TRIGGER").title, content: getBlock("TRIGGER").content },
  DIRECTIVE: { title: getBlock("DIRECTIVE").title, content: getBlock("DIRECTIVE").content },
  INSTRUCTION: { title: getBlock("INSTRUCTION").title, content: getBlock("INSTRUCTION").content },
  SUBJECT: { title: getBlock("SUBJECT").title, content: getBlock("SUBJECT").content },
};
Then include it in the NeoBlock object:
Copy code
Ts
const nb: NeoBlock = {
  id: nanoid(),
  sourceBlockIds: blocks.map(b => b.id),
  createdAt: Date.now(),
  label: "NeoBlock",
  snapshot,
};
✅ This preserves artifact immutability: editing live blocks won’t change old NeoBlocks.
3) Update duplicateNeoBlock() in src/umg/store.ts
When duplicating, carry snapshot forward:
Copy code
Ts
const dup: NeoBlock = {
  id: nanoid(),
  sourceBlockIds: [...src.sourceBlockIds],
  createdAt: Date.now(),
  label: "NeoBlock (Copy)",
  snapshot: src.snapshot, // <-- add this
};
4) Update commitCompose() in src/umg/store.ts (merge/bundle artifact snapshot)
When creating newNeo, include a snapshot. For v0, the simplest correct rule is:
MERGE: choose the “dominant” snapshot based on governancePriority (>=0.5 → A wins else B)
BUNDLE: also choose dominant (since it’s an artifact wrapper)
Add:
Copy code
Ts
const dominant = preview.governancePriority >= 0.5 ? A : B;
Then in newNeo:
Copy code
Ts
const newNeo: NeoBlock = {
  id: nanoid(),
  sourceBlockIds: [...new Set([...A.sourceBlockIds, ...B.sourceBlockIds])],
  createdAt: Date.now(),
  label: `${mergedLabel} • ov=${preview.semanticOverlap.toFixed(2)} gp=${preview.governancePriority.toFixed(2)}`,
  snapshot: dominant.snapshot, // <-- add this
};
✅ Strict and deterministic. You can get fancier later.
NEXT CHUNK 2 — Expand/Collapse state for NeoBlocks
5) Update src/umg/store.ts with expand state
Add to state type:
Copy code
Ts
expandedNeoBlockIds: Record<string, boolean>;
toggleNeoBlockExpanded: (neoBlockId: string) => void;
Add initial value:
Copy code
Ts
expandedNeoBlockIds: {},
Add implementation:
Copy code
Ts
toggleNeoBlockExpanded: (neoBlockId) => {
  const cur = get().expandedNeoBlockIds[neoBlockId] ?? false;
  set({
    expandedNeoBlockIds: {
      ...get().expandedNeoBlockIds,
      [neoBlockId]: !cur,
    },
  });
},
NEXT CHUNK 3 — NeoBlock node type (composite, expandable)
Right now you only have basic. We’ll add a second node type: neoblock.
6) Create src/umg/graph/NeoBlockNode.tsx
Copy code
Tsx
import React from "react";
import { useUmgStore } from "../store";
import type { MoltRole } from "../types";

const ORDER: MoltRole[] = ["TRIGGER", "DIRECTIVE", "INSTRUCTION", "SUBJECT"];

export function NeoBlockNode({ data }: any) {
  const {
    expandedNeoBlockIds,
    toggleNeoBlockExpanded,
    toggleSelectNeoBlock,
    selectedNeoBlockIds,
  } = useUmgStore();

  const neoBlockId: string = data.neoBlockId;
  const selected = selectedNeoBlockIds.includes(neoBlockId);
  const expanded = expandedNeoBlockIds[neoBlockId] ?? false;

  return (
    <div
      style={{
        padding: 10,
        border: selected ? "2px solid #000" : "1px solid #333",
        borderRadius: 14,
        background: "white",
        minWidth: 280,
        boxShadow: selected ? "0 4px 18px rgba(0,0,0,0.12)" : "none",
      }}
    >
      <div style={{ display: "flex", justifyContent: "space-between", gap: 10 }}>
        <div>
          <div style={{ fontWeight: 900 }}>{data.label}</div>
          <div style={{ fontSize: 12, opacity: 0.8 }}>
            lineage: {data.lineageCount} blocks
          </div>
        </div>

        <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
          <button
            onClick={() => toggleSelectNeoBlock(neoBlockId)}
            style={{
              padding: "6px 10px",
              borderRadius: 999,
              border: selected ? "2px solid #000" : "1px solid #ccc",
              background: "white",
              cursor: "pointer",
              fontSize: 12,
              fontWeight: 800,
            }}
            title="Select (pick 2 for Merge/Bundle)"
          >
            {selected ? "Selected" : "Select"}
          </button>

          <button
            onClick={() => toggleNeoBlockExpanded(neoBlockId)}
            style={{
              width: 34,
              height: 34,
              borderRadius: 10,
              border: "1px solid #ccc",
              background: "white",
              cursor: "pointer",
              fontWeight: 900,
            }}
            title={expanded ? "Collapse" : "Expand"}
          >
            {expanded ? "▾" : "▸"}
          </button>
        </div>
      </div>

      {expanded ? (
        <div style={{ marginTop: 10, borderTop: "1px solid #eee", paddingTop: 8 }}>
          <div style={{ fontSize: 12, fontWeight: 800, marginBottom: 6 }}>
            Internal MOLT Snapshot (read-only)
          </div>

          <div style={{ display: "grid", gap: 6 }}>
            {ORDER.map((r) => (
              <div
                key={r}
                style={{
                  display: "flex",
                  gap: 10,
                  alignItems: "center",
                  padding: "6px 8px",
                  border: "1px solid #eee",
                  borderRadius: 10,
                  background: "#fafafa",
                }}
              >
                <div style={{ width: 96, fontWeight: 900, fontSize: 11 }}>{r}</div>
                <div style={{ fontSize: 12, opacity: 0.9 }}>
                  {data.roleTitles?.[r] ?? "—"}
                </div>
              </div>
            ))}
          </div>

          <div style={{ marginTop: 10, fontSize: 11, opacity: 0.75 }}>
            NeoBlocks are artifacts. Edit live blocks on the MOLT ladder, then compress to create a new NeoBlock.
          </div>
        </div>
      ) : null}
    </div>
  );
}
7) Register the node type in src/umg/graph/nodeTypes.tsx
Add:
Copy code
Tsx
import { NeoBlockNode } from "./NeoBlockNode";
Then extend your export:
Copy code
Ts
export const nodeTypes = {
  basic: BasicNode,
  neoblock: NeoBlockNode,
};
✅ No TS issues.
NEXT CHUNK 4 — Render NeoBlocks using the new node type
8) Update src/umg/graph/buildNodes.ts
In the NeoBlocks section, change nodes from type: "basic" to type: "neoblock" and provide snapshot titles.
Replace your NeoBlocks loop with:
Copy code
Ts
neoBlocks.forEach((nb, i) => {
  nodes.push({
    id: `nb-${nb.id}`,
    type: "neoblock",
    position: { x: nbX, y: nbY0 + i * nbGap },
    data: {
      neoBlockId: nb.id,
      label: nb.label,
      lineageCount: nb.sourceBlockIds.length,
      roleTitles: {
        TRIGGER: nb.snapshot.TRIGGER.title,
        DIRECTIVE: nb.snapshot.DIRECTIVE.title,
        INSTRUCTION: nb.snapshot.INSTRUCTION.title,
        SUBJECT: nb.snapshot.SUBJECT.title,
      },
    },
  });
});
✅ NeoBlocks now show expandable internal structure.
Quick sanity notes (strict TS + safety)
We did not add node.style (often breaks ReactFlow node typing under strict TS).
Snapshot creation uses typed snapshot object, no any.
Expand state is plain Record<string, boolean>.
Next after this (you’ll like it)
Two optional upgrades that are still safe:
Visual step progress bar (top of sidebar) that reacts to tutorialStep
Persistence (localStorage hydration) so refresh doesn’t wipe state
If you say “continue” again, I’ll default to persistence next, because it makes creator mode feel real instantly.