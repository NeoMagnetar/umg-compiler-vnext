
Got it. Ignore mobile. We’ll do three laptop-only fixes, minimal code churn:
Tutorial Output in Block Tutorial can collapse (drop down/up) so it stops blocking the graph
Block Tutorial Output UI matches Creator Output (same tabs + layout)
Live JSON “Active State” panel updates as you click/select nodes
Here’s the exact spec + code-shape for the Replit agent.
1) Make Tutorial Output collapsible (drop down/up)
Behavior
Output sits at the bottom/right (wherever it is now), but has a header bar with:
Title: “Output”
Toggle: Collapse / Expand (chevron)
Collapsed: shows only the header (like 36–44px tall)
Expanded: shows tabs + content
State persists during session (optional: persist in localStorage)
Implementation (quick)
Create a generic wrapper:
src/umg/ui/CollapsiblePanel.tsx (new)
Props: title, defaultOpen, children
Local useState(open)
Then wrap the tutorial output panel in it.
2) Clone Creator Output UI into Block Tutorial
Behavior
Block Tutorial output should look and behave exactly like Creator:
Tabs: Runtime | Trace | Raw
Same tab styling + spacing
Same “Raw JSON” presentation
Implementation (best low-cost)
Extract shared output component from Creator and reuse:
If Creator has CompileOutputPanel.tsx:
Split into:
OutputTabs.tsx (tab UI)
OutputContent.tsx (runtime/trace/raw render)
Then in tutorial mode, render the same component but pass “tutorial data”.
If you don’t want refactor: copy the component into tutorial for now (cheapest) and unify later.
3) “Active JSON” panel that updates when clicking blocks
What you want
When you click/select any node in Tutorial graph, show “what happened” and the current active JSON. That means:
Show selected object (Block / NeoBlock / NeoStack / Sleeve)
Show key workspace status:
tutorialStep
spineComplete
readyToCompress
neoBlocks count, neoStacks count, sleeve exists
Show raw store slices (optional)
Minimal version (fast)
Add a new tab inside the Output UI called: Active (Or put it in Raw, but Active is clearer.)
Active tab shows:
Copy code
Json
{
  "selected": { ... },
  "workspace": { ... },
  "counts": { ... }
}
Where it comes from
From store selectors:
selectedNodeId / selectedKind
arrays (blocks, neoBlocks, neoStacks)
sleeve, runtimeSpec, trace, tutorialStep
Then compute:
selectedObject by looking up the selected ID in those collections
Copy/paste instructions for Replit agent (exact)
Task: Laptop-only output cleanup for Block Tutorial
Add collapsible behavior to Tutorial Output so it can drop down/up and not block graph:
Create src/umg/ui/CollapsiblePanel.tsx (header bar + chevron + open/closed state)
Wrap the tutorial output panel in it
Collapsed height ~40px, expanded shows full content
Make Block Tutorial output UI match Creator output UI:
Reuse Creator CompileOutputPanel UI (tabs + styling)
If reuse is hard, duplicate it into src/umg/ui/TutorialCompileOutputPanel.tsx but keep identical UI.
Tabs: Runtime | Trace | Raw
Add “Active” JSON view that updates as user clicks/selects nodes:
Add an “Active” tab (Active | Runtime | Trace | Raw) OR embed Active section at top of Raw.
Active JSON includes:
selected object (Block/NeoBlock/NeoStack/Sleeve)
tutorialStep + readiness booleans
counts of blocks/neoBlocks/neoStacks + sleeve present
Render as pretty JSON (JSON.stringify(obj, null, 2)) in monospace.
Return changed files:
CreatorMode.tsx or tutorial shell where output is mounted
New CollapsiblePanel.tsx
Tutorial output panel file (updated)
Any shared output UI file (if extracted)
Acceptance:
Output can collapse/expand.
UI matches Creator output look/feel.
Clicking different nodes updates Active JSON immediately.
Tiny implementation detail that prevents headaches
When you add the Active JSON tab, make sure it doesn’t stringify the entire store (could be huge). Only stringify:
the selected object
a small workspace summary object
That keeps performance snappy.
If you paste the current TutorialOutputPanel.tsx (or whatever file renders tutorial output), I can point to the exact place to wrap with CollapsiblePanel and where to inject the Active tab.


// src/umg/ui/CollapsiblePanel.tsx
import React, { useEffect, useState } from "react";

type Props = {
  title: string;
  defaultOpen?: boolean;
  storageKey?: string; // optional: persist open/closed
  right?: React.ReactNode; // optional right-side header content
  children: React.ReactNode;
  style?: React.CSSProperties;
};

export function CollapsiblePanel({
  title,
  defaultOpen = true,
  storageKey,
  right,
  children,
  style,
}: Props) {
  const [open, setOpen] = useState(defaultOpen);

  useEffect(() => {
    if (!storageKey) return;
    const raw = localStorage.getItem(storageKey);
    if (raw === "open") setOpen(true);
    if (raw === "closed") setOpen(false);
  }, [storageKey]);

  useEffect(() => {
    if (!storageKey) return;
    localStorage.setItem(storageKey, open ? "open" : "closed");
  }, [open, storageKey]);

  return (
    <div style={{ borderTop: "1px solid rgba(255,255,255,0.1)", background: "#0d0d12", ...style }}>
      <div
        style={{
          height: 40,
          display: "flex",
          alignItems: "center",
          justifyContent: "space-between",
          padding: "0 10px",
          userSelect: "none",
          cursor: "pointer",
        }}
        onClick={() => setOpen((v) => !v)}
      >
        <div style={{ display: "flex", gap: 8, alignItems: "center" }}>
          <span style={{ fontWeight: 800, fontSize: 12, color: "rgba(255,255,255,0.9)" }}>{title}</span>
          <span style={{ fontSize: 12, opacity: 0.7 }}>{open ? "▾" : "▸"}</span>
        </div>
        <div onClick={(e) => e.stopPropagation()}>{right}</div>
      </div>
      {open ? <div style={{ padding: 10 }}>{children}</div> : null}
    </div>
  );
}

// src/umg/ui/TutorialOutputPanel.tsx
import React, { useMemo, useState } from "react";
import { CollapsiblePanel } from "./CollapsiblePanel";
import { useUmgStore } from "../store";
import { computeTutorialStep } from "../tutorial";
import type { Block, NeoBlock, NeoStack, Sleeve } from "../types";

type Tab = "Active" | "Runtime" | "Trace" | "Raw";

function pretty(obj: any) {
  try {
    return JSON.stringify(obj, null, 2);
  } catch {
    return String(obj);
  }
}

function findSelected(
  selectedNodeId: string | null,
  blocks: Block[],
  neoBlocks: NeoBlock[],
  neoStacks: NeoStack[],
  sleeve: Sleeve | null
) {
  if (!selectedNodeId) return null;

  const b = blocks.find((x) => x.id === selectedNodeId);
  if (b) return { kind: "BLOCK" as const, data: b };

  const nb = neoBlocks.find((x) => x.id === selectedNodeId);
  if (nb) return { kind: "NEOBLOCK" as const, data: nb };

  const ns = neoStacks.find((x) => x.id === selectedNodeId);
  if (ns) return { kind: "NEOSTACK" as const, data: ns };

  if (sleeve && sleeve.id === selectedNodeId) return { kind: "SLEEVE" as const, data: sleeve };

  return { kind: "UNKNOWN" as const, data: { id: selectedNodeId } };
}

export function TutorialOutputPanel() {
  const [
    blocks,
    neoBlocks,
    neoStacks,
    sleeve,
    runtimeSpec,
    trace,
    tutorialStep,
    selectedNodeId,
  ] = useUmgStore((s) => [
    s.blocks,
    s.neoBlocks,
    s.neoStacks,
    s.sleeve,
    (s as any).runtimeSpec ?? null,
    (s as any).trace ?? null,
    (s as any).tutorialStep ?? "EMPTY",
    (s as any).selectedNodeId ?? null,
  ]);

  const [tab, setTab] = useState<Tab>("Active");

  const active = useMemo(() => {
    const sel = findSelected(selectedNodeId, blocks, neoBlocks, neoStacks, sleeve);
    const derivedStep = computeTutorialStep(blocks, neoBlocks, neoStacks, sleeve, runtimeSpec);

    const spineComplete = new Set(blocks.map((b) => b.role)).size >= 7; // if you have getSpineBlocks, use it instead
    const readyToCompress = spineComplete && neoBlocks.length === 0;
    const composeReady = neoBlocks.length >= 2;

    return {
      selected: sel,
      workspace: {
        tutorialStep,
        derivedStep,
        spineComplete,
        readyToCompress,
        composeReady,
      },
      counts: {
        blocks: blocks.length,
        neoBlocks: neoBlocks.length,
        neoStacks: neoStacks.length,
        sleeve: !!sleeve,
      },
    };
  }, [blocks, neoBlocks, neoStacks, sleeve, runtimeSpec, tutorialStep, selectedNodeId]);

  // Minimal “Creator-like” tabs
  const tabs: Tab[] = ["Active", "Runtime", "Trace", "Raw"];

  const body = useMemo(() => {
    if (tab === "Active") return <JsonBox value={active} />;
    if (tab === "Runtime") return <JsonBox value={runtimeSpec ?? { note: "No runtimeSpec yet." }} />;
    if (tab === "Trace") return <JsonBox value={trace ?? { note: "No trace yet." }} />;
    return <JsonBox value={{ blocks, neoBlocks, neoStacks, sleeve }} />;
  }, [tab, active, runtimeSpec, trace, blocks, neoBlocks, neoStacks, sleeve]);

  return (
    <CollapsiblePanel title="Output" defaultOpen={false} storageKey="umg:tutorial:output:open">
      <div style={{ display: "flex", gap: 6, marginBottom: 8 }}>
        {tabs.map((t) => (
          <button
            key={t}
            onClick={() => setTab(t)}
            style={{
              padding: "8px 10px",
              fontSize: 12,
              fontWeight: 700,
              borderRadius: 10,
              border: "1px solid rgba(255,255,255,0.12)",
              background: tab === t ? "rgba(96,165,250,0.18)" : "rgba(255,255,255,0.04)",
              color: "rgba(255,255,255,0.9)",
              cursor: "pointer",
            }}
          >
            {t}
          </button>
        ))}
      </div>

      {body}
    </CollapsiblePanel>
  );
}

function JsonBox({ value }: { value: any }) {
  return (
    <pre
      style={{
        margin: 0,
        whiteSpace: "pre-wrap",
        fontSize: 12,
        lineHeight: 1.35,
        background: "#0b0b10",
        border: "1px solid rgba(255,255,255,0.10)",
        borderRadius: 12,
        padding: 10,
        maxHeight: 320,
        overflow: "auto",
        color: "rgba(255,255,255,0.88)",
      }}
    >
      {pretty(value)}
    </pre>
  );
}

// Patch point: wherever Block Tutorial currently renders <TutorialOutputPanel />
// Wrap it so it does NOT steal graph space. Put it BELOW the graph container, not inside it.
// Example layout snippet (adjust to your file, likely CreatorMode.tsx / TutorialMode.tsx):

/*
<div style={{ display:"flex", flexDirection:"column", height:"100%" }}>
  <div style={{ flex: 1, minHeight: 0 }}>
    <ReactFlow ... />
  </div>
  <TutorialOutputPanel />
</div>
*/

/* OPTIONAL: if you want it to feel closer to Creator mode */
button:active { transform: scale(0.99); }

Notes for your agent (tiny):
If your store uses selectedNodeId under a different name (selectedBlockId, selectedGraphNodeId), swap that in the selector.
If computeTutorialStep signature differs, remove derivedStep or adjust call.
If you already have runtimeSpec/trace types, replace any usages.

