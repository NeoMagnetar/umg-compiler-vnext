Yep — your agent is correctly diagnosing it: React Flow edge warnings happen when a custom node has no <Handle />s (or the edge is targeting a node type that doesn’t render handles). And persistence should be done with zustand persist + versioned migrations, not hand-rolled localStorage spaghetti.
Below is the clean, strict-TS-safe patch for both:
Fix node connection errors (add handles to custom nodes / ensure all edge endpoints exist)
Add persistence (versioned schema + reset)
I’ll keep this in small, Replit-friendly code chunks.
1) Fix React Flow node connection errors
A) Add Handles to BasicNode and NeoBlockNode
Even if you don’t visually want ports, they can be invisible.
Edit: src/umg/graph/nodeTypes.tsx
At the top, import Handle:
Copy code
Tsx
import { Handle, Position } from "reactflow";
Inside BasicNode’s returned JSX, add handles near the top-level div:
Copy code
Tsx
<div
  onClick={() => selectNode(id)}
  style={{ ... }}
>
  {/* Connection handles */}
  <Handle type="target" position={Position.Left} style={{ opacity: 0 }} />
  <Handle type="source" position={Position.Right} style={{ opacity: 0 }} />

  <div style={{ fontWeight: 800 }}>{data.title}</div>
  ...
</div>
That alone fixes most warnings for edges connected to basic nodes.
Edit: src/umg/graph/NeoBlockNode.tsx
Import Handle:
Copy code
Tsx
import { Handle, Position } from "reactflow";
Add these inside the outermost wrapper div:
Copy code
Tsx
<Handle type="target" position={Position.Left} style={{ opacity: 0 }} />
<Handle type="source" position={Position.Right} style={{ opacity: 0 }} />
Now edges can connect to NeoBlock nodes cleanly.
B) Ensure edges target the correct node IDs
React Flow will warn if an edge’s source/target references a node id that doesn’t exist.
In src/umg/graph/buildNodes.ts, make sure you always build nodes before edges that reference them, and use consistent IDs:
NeoBlock node id: nb-${nb.id}
NeoStack node id: ns-${stack.id}
Sleeve node id: sl-${sleeve.id}
MOLT ladder nodes: role-${role}
If you build edges dynamically (e.g., “newestNeoBlock”), guard it:
Copy code
Ts
if (newestNb && lastStack) {
  edges.push(smoothEdge(`e-nb-${newestNb.id}-ns-${lastStack.id}`, `nb-${newestNb.id}`, `ns-${lastStack.id}`));
}
2) Add persistence with versioned schema (Zustand persist)
This is the safest way: persist only the data, not functions.
A) Install (if needed)
Copy code
Bash
npm i zustand
(You already have it.)
Zustand persist is built-in: zustand/middleware.
B) Update src/umg/store.ts to use persist
1) Change your imports at the top
Copy code
Ts
import { create } from "zustand";
import { persist, createJSONStorage } from "zustand/middleware";
2) Wrap the store with persist
Replace:
Copy code
Ts
export const useUmgStore = create<State>((set, get) => ({
  ...
}));
With:
Copy code
Ts
const STORAGE_KEY = "umg.v0.creator";
const STORAGE_VERSION = 1;

export const useUmgStore = create<State>()(
  persist(
    (set, get) => ({
      ... // your existing state + actions
    }),
    {
      name: STORAGE_KEY,
      version: STORAGE_VERSION,
      storage: createJSONStorage(() => localStorage),

      // only persist data, not functions
      partialize: (state) => ({
        tutorialStep: state.tutorialStep,

        blocks: state.blocks,
        selectedBlockId: state.selectedBlockId,

        neoBlocks: state.neoBlocks,
        selectedNeoBlockIds: state.selectedNeoBlockIds,

        preview: state.preview,
        lastComposeMode: state.lastComposeMode,

        neoStacks: state.neoStacks,
        sleeve: state.sleeve,

        // if you have these now:
        expandedNeoBlockIds: state.expandedNeoBlockIds,

        runtimeSpec: state.runtimeSpec,
        trace: state.trace,
      }),

      // future-proofing: migrations between versions
      migrate: (persisted: any, version) => {
        // v0 -> v1 example
        if (version === 0) {
          return {
            ...persisted,
            expandedNeoBlockIds: persisted.expandedNeoBlockIds ?? {},
          };
        }
        return persisted;
      },
    }
  )
);
✅ This is strict-safe.
✅ partialize prevents weirdness with persisting methods.
C) Add a reset action (and wire it to a sidebar button)
1) Add to State type
Copy code
Ts
resetAll: () => void;
2) Add implementation near the bottom of store actions
This should reset to your initial defaults:
Copy code
Ts
resetAll: () => {
  // optional: clear storage explicitly
  localStorage.removeItem("umg.v0.creator");

  set({
    tutorialStep: "EMPTY",
    blocks: [],
    selectedBlockId: null,

    neoBlocks: [],
    selectedNeoBlockIds: [],

    preview: { semanticOverlap: 0.5, governancePriority: 0.5 },
    lastComposeMode: null,

    neoStacks: [],
    sleeve: null,

    expandedNeoBlockIds: {},

    runtimeSpec: null,
    trace: null,
    selectedNodeId: null,
  });
},
3) Add a Reset button to src/umg/ui/CreatorSidebar.tsx
Place it near the top:
Copy code
Tsx
<button onClick={s.resetAll} style={btn}>
  Reset v0 State
</button>
<div style={hint}>Clears local progress and restarts the workflow.</div>
Quick verification (no vibes, only checks)
Edge warnings gone
Open console → no “can’t create edge” / handle warnings
Edges connect to NeoBlocks
NeoBlock → NeoStack edge shows consistently
Persistence works
Create NeoBlock, expand it, refresh page
NeoBlock still exists
Expanded state persists if you included expandedNeoBlockIds in partialize
Reset works
Click reset → graph returns to empty, storage cleared
Refresh → still empty
Exact files to edit next (your requested list)
src/umg/graph/nodeTypes.tsx — add <Handle />s to BasicNode
src/umg/graph/NeoBlockNode.tsx — add <Handle />s to NeoBlock node
src/umg/graph/buildNodes.ts — guard edges + ensure IDs consistent (if needed)
src/umg/store.ts — add persist() wrapper + partialize + migrate + resetAll
src/umg/ui/CreatorSidebar.tsx — add Reset button (and optionally show “Saved” indicator)
If you want the next upgrade after this: a tiny “Save status” indicator that shows hydrated state from zustand persist (so you know you’re not hallucinating persistence). That’s a 10-line patch.