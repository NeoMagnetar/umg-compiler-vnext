UMG Studio — STEP 3B (MOLT blocks clickable + editable details + tags)
SCOPE: UI/data-layer only. Keep existing behavior. No compiler changes.

HARD RULES
- ONLY edit: apps/umg-studio/**
- DO NOT edit: compiler-v0/**, server/**, client/**, shared/**, root package.json, .replit, workflows, ports
- Keep “Default” button behavior exactly the same (do not change what it does)
- Keep existing compile flow exactly the same
- Must compile TypeScript with no errors

GOAL
In the MOLT STACK view, blocks are clickable.
Clicking a block opens BottomPanel editor where user can:
- Edit Title (editable field)
- Edit Block Info (content + optional description fields if already supported)
- Add/Remove Tags (using existing tag format)
Edits must update the Sleeve JSON the same way current JSON editor updates do.
After compile, tag highlighting behavior must remain (do not replace it).

WHAT “BLOCK INFO” MEANS
Use the existing Block schema fields:
- title (editable)
- content (editable)
- tags (editable list of strings)
If there are existing metadata fields already used in your UI (e.g., role/moltType), display read-only unless already editable today.

IMPLEMENTATION PLAN

1) Make MOLT blocks clickable + select
File: apps/umg-studio/src/components/graphs/MoltGraphView.tsx
(or wherever the MOLT graph nodes are rendered)

- Ensure each rendered block node has an onClick handler.
- On click:
  - setSelectedNode({ kind: "block", blockId, stackId, moltType }) OR reuse existing selection state if present
  - open BottomPanel (if collapsed) and switch it to the editor tab (Details or new Edit tab)
- Visual: selected block should show a clear outline/glow (minimal CSS ok).
- IMPORTANT: do not break scrolling, tapping, or existing graph layout.

2) Create an editor UI for the selected block
Add new component:
File: apps/umg-studio/src/components/BlockDetailsEditor.tsx (new)

Props:
- sleeveJson: string
- selectedBlockId: string | null
- onChangeSleeveJson(nextJson: string): void

UI requirements:
- Title input (editable)
- Content textarea (editable)
- Tags editor:
  - Show current tags as chips
  - Input to add a tag (enter key or “Add” button)
  - Each chip has an “x” remove
  - Normalize: trim whitespace, ignore empty, prevent duplicates (case-insensitive ok)
- Show moltType and block id read-only (helpful context)

Update rules:
- All edits write back to sleeveJson using the SAME update mechanism used by BlockInspector today.
- If there is an existing helper like updateBlock(...) in:
  apps/umg-studio/src/lib/sleeveEdit.ts
  use it. If not, create a minimal helper there:
  updateBlock(json, blockId, patch) -> { nextJson?, error? }
- After applying edits, changes must be visible in:
  - JsonEditor (Sleeve JSON)
  - Any existing “current block” inspector data
  - Compile results (because compiler reads sleeveJson)

3) Wire editor into BottomPanel (single place to edit)
File: apps/umg-studio/src/components/BottomPanel.tsx

- Add a tab called: “EDIT” (or reuse existing “Details” tab if it already behaves like editing)
- In this tab, render <BlockDetailsEditor …> when a block is selected.
- If no block selected, show “Tap a block to edit.”

Mobile behavior:
- Do NOT force BottomPanel open on every tap if that becomes annoying.
- But clicking a block should at least select it and show its details when user expands BottomPanel.
(If BottomPanel is already expanded, switch to EDIT tab.)

4) Keep “Default” button behavior unchanged
Wherever “Default” button is:
- DO NOT change its handler.
- If “Default” currently selects/creates a block template, keep it exactly the same.
- Only add click-to-select + edit flow on block nodes.

5) Keep tag highlighting behavior unchanged
- Do NOT rewrite TagExplorer/Tag highlighting logic.
- Our edits only update block.tags in sleeveJson.
- After compile, any existing highlight behavior should continue to work automatically.

6) Verification checklist (Agent must report)
- Click a MOLT block -> it becomes selected + shows in BottomPanel EDIT tab
- Editing title/content/tags updates sleeveJson immediately
- JsonEditor reflects the change
- Compile still works (no logic change)
- Existing tag highlighting still works after compile
- TypeScript build passes

DELIVERABLES
- List files changed (paths only)
- Brief confirmation checklist above
STOP after completion (no extra features).