Replit Agent Task — Phase 9: Make Ops Real (Derived Sleeve) + NeoBlock/NeoStack Insert + Export/Import
Hard rules:
- Do NOT modify compiler-v0/**
- Sleeve JSON remains source of truth
- Ops remain stored under sleeve.ui.ops
- Any derived artifacts must live under sleeve.ui.derived (compiler ignores)

A) Implement applyOps() to generate a derived sleeve (editor-only)
1) Create: apps/umg-studio/src/lib/applyOps.ts
Export:
- applyOpsToSleeveJson(json: string): { nextJson?: string; error?: string; report?: ApplyOpsReport }
Rules:
- Parse sleeveJson.
- Validate ops: each op requires >=2 blocks, same stack, same lane.
- For each Bundle op: create ONE new block (moltType = lane) with:
  - id: "bund_" + shortid
  - title: "Bundle: <op.name or count>"
  - content: concatenation of source blocks content with separators
  - tags: union of tags + ["bundled"]
  - meta stored in sleeve.ui.derived.map[newId] = { opId, sourceBlockIds }
  - Remove source blockIds from that stack.blockIds, insert newId in their place
- For each Merge op: same pattern but tag ["merged"] and title "Merge: ..."
- Do NOT delete original blocks from sleeve.blocks[] (keep them for now), only change stack.blockIds ordering to point at derived blocks.
- Return JSON.stringify(derivedSleeve, null, 2) and a report: counts changed.

B) Add “Compile Raw” vs “Compile With Ops”
1) Edit: apps/umg-studio/src/App.tsx (or wherever compile button lives)
- Add two buttons:
  - Compile (Raw): current behavior
  - Compile (With Ops): runs applyOpsToSleeveJson(sleeveJson) and compiles using the derivedJson
- Store lastCompiledJson separately for raw/ops or store a flag + lastCompiledSource.
2) In RuntimeViewer add a small summary box:
- Mode: Raw / With Ops
- Bundles applied: X
- Merges applied: Y
- Net blockIds change: +/-N

C) NeoBlock Insert (make stub real)
1) Edit: apps/umg-studio/src/components/LibraryPanel.tsx (NeoBlocks tab)
- Add “Insert NeoBlock into Stack” button.
- Behavior: instantiate blocks referenced in the NeoBlock lanes into the chosen stack:
  - Create new blocks by cloning source blocks (from current sleeve if present) OR if missing, create placeholder blocks with title "(missing template)".
  - Mint new ids.
  - Insert into stack.blockIds grouped by lane (moltType), preserving lane order.

D) NeoStack Insert into Sleeve
1) NeoStacks tab: add “Insert NeoStack into current sleeve”
- For each NeoBlock in the neostack: create a new stack (or add to selected stack; simplest = create new stack)
- Insert instantiated blocks accordingly.

E) Export/Import (portable)
1) Add to LibraryPanel top-right:
- Export Library JSON (blocks/neoblocks/neostacks/sleeves)
- Import Library JSON (paste modal)
2) Add to RightPanel or TopBar:
- Export current sleeveJson (download)
- Import sleeveJson (paste to replace)

Deliverables:
- Compile (With Ops) changes runtime output in visible ways
- NeoBlock insert actually populates canvas
- NeoStack insert creates stacks/blocks
- Export/Import works with a single JSON blob
- List files changed (paths only) + confirm build passes