Replit Agent Task — Sidebar dropdown reorder + move “Template” content (UI only)

Hard rules
- UI changes only (apps/umg-studio)
- Do NOT touch compiler-v0/** or any core compiler files
- Keep TS compiling, no behavior regressions

Goal
1) Reorder Left sidebar sections:
   - Structure dropdown to TOP
   - Template dropdown to BOTTOM (last)
   - Keep Library and Tags in between (unless you already have a different set—preserve existing sections, just reorder these relative positions)

2) Move “Library Template” UI + its info OUT of Library section and INTO Template section

Files to inspect / likely edit
- apps/umg-studio/src/components/LeftPanel.tsx
- apps/umg-studio/src/components/LibraryPanel.tsx
- apps/umg-studio/src/components/StructurePanel.tsx
- apps/umg-studio/src/components/TagExplorer.tsx (if used)
- apps/umg-studio/src/components/TemplatePanel.tsx (if exists; otherwise create)

Part A — Reorder sidebar sections (LeftPanel)
A1) In LeftPanel.tsx, find the sidebar accordion/sections list.
A2) Reorder to this exact visual order:
   1. STRUCTURE
   2. BLOCK LIBRARY
   3. TAGS
   4. TEMPLATE
Notes:
- Do NOT change the internal IDs/keys used for persistence (localStorage).
- If you have localStorage like `umg.sidebar.sections`, keep the same keys but ensure “template” renders last and “structure” renders first.

Part B — Move “Library Template” content into Template section
B1) Identify what “library template” means in the current UI:
   - Any “Template” header, helper text, instructions, or template-related controls currently inside LibraryPanel
   - Any localStorage/store actions tied to “templates” that are currently shown in LibraryPanel UI

B2)️) Remove that template UI from LibraryPanel.tsx:
   - Keep the core Library features intact: blocks list, filters, insert actions, etc.
   - Only remove the template-related panel/section/content.

B3) Create/Update Template section UI:
Option 1 (preferred): Create `apps/umg-studio/src/components/TemplatePanel.tsx`
- Move the removed template UI into this component
- Keep the same props/state wiring that it previously used inside LibraryPanel (do not rewrite logic unless necessary)

Option 2: If Template UI already exists in LeftPanel, inject the moved UI there directly.

B4) Wire TemplatePanel into LeftPanel.tsx under the TEMPLATE accordion.

Part C — Defaults (don’t change unless already implemented)
- Keep existing default open/closed behavior as-is (whatever you already have).
- Only reorder and relocate the template UI.

Verification checklist (must confirm)
- Structure accordion appears at the top of the left sidebar
- Template accordion appears at the bottom (last)
- Library no longer contains the “template” subsection
- Template accordion now contains the moved “library template” UI and it still works
- No TypeScript errors, app loads normally on desktop + mobile

Commit
- Commit message: `UI: reorder sidebar + move template UI out of library`
- List files changed (paths only) in the agent summary
```0