TASK: Add a new derived output called RuntimePromptSpec to packages/compiler-v0/ so the compiler produces a deterministic “what the model would actually see” prompt structure per NeoBlock (stack), built strictly from existing compiled results (active selections + resolved ordering).
HARD RULES
Do NOT touch anything in apps/.
Do NOT change logic in these files:
normalizeSegments.ts, applyMerges.ts, applyBundles.ts, applyGovernance.ts, resolveAuthority.ts, selectPrimary.ts, selectDirective.ts, selectInstruction.ts, selectBlueprint.ts, buildNeoStacks.ts
Keep determinism absolute (stable ordering, no randomness).
Snapshots must remain deterministic (timestamps already normalized).
Only add minimal new code + 1 sample + 1 snapshot.
1) Update src/types.ts
Add these types near RuntimeSpec / runtime outputs:
Copy code
Ts
export type PromptSectionType =
  | "trigger"
  | "directive"
  | "instruction"
  | "subject"
  | "primary"
  | "philosophy"
  | "blueprint";

export interface PromptSection {
  type: PromptSectionType;
  blockIds: string[];      // ids included in this section, in display order
  text: string;            // joined block contents, deterministic
}

export interface RuntimePromptSpec {
  sleeveId: string;
  neoBlockPrompts: Array<{
    neoBlockId: string;
    stackId: string;
    sections: PromptSection[];
    fullText: string;      // concatenation of section.text in section order
  }>;
}
Extend RuntimeSpec with:
Copy code
Ts
promptSpec: RuntimePromptSpec;
No schema drift anywhere else. Do not add a second types file.
2) Create src/buildPromptSpec.ts (NEW)
Create a new module that exports:
Copy code
Ts
import type { RuntimeNeoBlock, Block, MoltType, RuntimePromptSpec, PromptSection } from "./types.js";
Function signature:
Copy code
Ts
export function buildPromptSpec(args: {
  sleeveId: string;
  neoBlocks: RuntimeNeoBlock[];
  blocksById: Map<string, Block>;
}): RuntimePromptSpec
Required behavior (deterministic):
For each neoBlock:
Create sections in this exact order:
trigger
directive
instruction
subject
primary
philosophy
blueprint
For each section, determine the block IDs to include:
Use neoBlock.active.* for these:
triggerIds
directiveIds
instructionIds
subjectIds
philosophyIds
blueprintIds
For primary, include [neoBlock.active.primaryId] if present else [].
Convert block IDs into text:
Look up each block in blocksById.
Use block.content exactly as stored.
Join within a section using two newlines: "\n\n".
If a section has zero blocks, still include it, but text should be empty string and blockIds empty array.
fullText is concatenation of section texts in section order, separated by:
"\n\n---\n\n" between sections that have non-empty text.
(Do not add separators for empty sections.)
No markdown magic, no extra formatting beyond that. Keep it boring and stable.
3) Wire into src/compile.ts
Pipeline placement:
After NeoBlocks + NeoStacks are built (since PromptSpec is derived from NeoBlocks).
So final area should do:
neoBlocks = buildNeoBlocks(...)
neoStacks = buildNeoStacks(...)
promptSpec = buildPromptSpec({ sleeveId: sleeve.id, neoBlocks, blocksById })
Attach promptSpec into runtime.
Do not change error behavior or existing trace codes.
Optionally add one trace info event:
INFO_PROMPT_SPEC_DONE (“Built promptSpec for N neoBlocks.”) But only if you already have a consistent trace emitter pattern for “info stages.” If adding this creates noise, skip it.
4) Add Sample + Snapshot
Create: packages/compiler-v0/samples/promptspec_example.json
Requirements:
Two stacks (stack_a, stack_b)
Each has at least:
1 primary
1 instruction
1 directive
1 blueprint
In stack_a, include directive alternates bundled (so directive selection chooses 1).
In stack_b, include 2 philosophy blocks (so both remain active).
No governance required (optional).
Then run:
npm run build
npm run snapshot
Ensure snapshots/promptspec_example.out.json is created and hasErrors=false.
5) Required Report Back (Replit must print)
After implementation print:
tree packages/compiler-v0/src
From snapshots/promptspec_example.out.json, print:
runtime.promptSpec.neoBlockPrompts.length
For the first neoBlock prompt:
sections.map(s => ({ type: s.type, blockIds: s.blockIds }))
first 300 chars of fullText
Confirm:
npm run build ✅ PASS
npm run snapshot ✅ PASS
STOP.