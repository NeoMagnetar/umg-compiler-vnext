{
  "name": "umg-compiler-root",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "npm --prefix compiler-v0 run build",
    "snapshot": "npm --prefix compiler-v0 run snapshot",
    "contract": "npm --prefix compiler-v0 run contract",
    "test": "npm --prefix compiler-v0 run test"
  }
}