# UMG Studio

Visual editor for UMG Sleeve configurations.

## Compiler Path Note

The compiler lives at `/compiler-v0/src` (do not use `packages/compiler-v0`).

The TypeScript alias `@compiler-v0` is configured in `tsconfig.json` to point to `../../compiler-v0/src/index.ts`.
